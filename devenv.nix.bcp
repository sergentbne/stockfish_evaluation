{ pkgs, lib, config, inputs, ... }:
{
	cachix.enable = false
  languages.python = {
		 
		version = "3.14";   
		enable = true;
		venv.enable = true	
		uv = {
			enable = true;
			sync.enable = true;
		};
	  
	};
}
